#include <bits/stdc++.h>

using namespace std;


int main() {
    int N = 1;

    int counter = 1;

    while (1) {
        cin >> N;

        if (!N) break;

        int o = N, d = 0, t = 0;

        int answ = 0;

        cout << "Teste " << counter << '\n';
        cout << (unsigned long long)(pow(2, N)) - 1 << "\n\n";

        counter++;
    }

    return 0;
}